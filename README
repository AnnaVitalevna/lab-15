Lab-15

Копирования файла с использованием библиотеки Java NIO (New Input/Output).

Порядок работы:
Запустить программу кликнув на файл lab-15.bat
В программе используются параметры командной строки, где необходимо указать директории:
	1) исходного фвйла;
	2) конечного файла,
и размера буффера.
После запуска программа копирует файл и выводит на экран время и прогресс копирования файла.